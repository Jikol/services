services:
  registry-ui:
    environment:
      REGISTRY_URL: http://${REGISTRY_PUBLIC_HOST:?error}
    ports: []
    networks:
      - proxy

  registry-server:
    ports: []
    networks:
      - proxy

networks:
  proxy:
    external: true

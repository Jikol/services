name: gitea

services:
  server:
    container_name: gitea
    image: gitea/gitea:1.21.6
    environment:
      - USER_UID=1000
      - USER_GID=1000
    ports:
      - "3000:3000"
      - "22:22"
    restart: unless-stopped
    volumes:
      - ./data:/data
